{
  "name": "@cube/windows",
  "version": "0.1.0",
  "private": true,
  "description": "Cube Solver Pro for Windows.",
  "main": "dist-electron/main.cjs",
  "scripts": {
    "build": "vite build",
    "build:electron": "node scripts/build-electron.mjs",
    "dev": "npm run build:electron && cross-env VITE_DEV_SERVER_URL=http://localhost:5173 electron .",
    "dist": "npm run build && npm run build:electron && electron-builder --win nsis",
    "dist:store": "npm run build && npm run build:electron && electron-builder --win appx",
    "smoke": "npm run build && npm run build:electron && node scripts/smoke-electron.cjs"
  },
  "dependencies": {
    "@cube/platform": "*",
    "@cube/ui": "*"
  },
  "devDependencies": {
    "electron": "^32.2.0",
    "electron-builder": "^25.1.8",
    "esbuild": "^0.24.0"
  }
}
